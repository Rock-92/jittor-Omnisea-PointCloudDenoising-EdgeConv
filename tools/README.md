# Tools

Put one-off data conversion, statistics, visualization, or sanity-check scripts
in this directory. Core training and inference code should stay in `src/`, and
user-facing run commands should stay in `scripts/`.
